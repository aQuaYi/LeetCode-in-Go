package Problem0115

func numDistinct(s string, t string) int {
	if len(s) < len(t) {
		return 0
	}

	res := 0
	lenS, lenT := len(s), len(t)

	var dfs func()
	dfs = func(sub string, cnt int) {

	}

	return res
}
